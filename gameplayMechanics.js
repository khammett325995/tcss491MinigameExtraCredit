//Used to add score updates
class gameplayMechanics {
    constructor() {
        var score = 0;
        var timer = 0;
    };

    //Collision Detection Code

    //Score Updating Code
    scoreUpdate() {

        //Within the gameplay loop.
        while (timer != 90) {
        //If a Copepod is collected, score += 100
        
        //if () {
            //score += 100;
        //};

        //If a Mysid Shrimp is collected, score += 400

        //if () {
        //    score += 400;
        //};
        };
    };
};